# 莱芜文旅小程序软件设计文档

本文档基于《莱芜文旅小程序软件需求规格说明》，提供了一个全面的软件设计方案。

## 1. 系统架构

本系统采用B/S（浏览器/服务器）架构，前后端分离。

- **前端（客户端）**: 微信小程序，内置一个地图服务适配器层，用于统一调用不同的地图SDK（腾讯、高德、百度）。
- **后端（服务器）**: 提供RESTful API接口，负责业务逻辑处理、数据存储与管理。
- **数据库**: 采用关系型数据库与非关系型数据库相结合的方式。

### 1.1 架构图

```
+----------------------+      +------------------------+      +---------------------+
|                      |      |                        |      |   MySQL Database    |
|   微信小程序 (Vue)    |----->|   后端 Spring Boot API  |----->| (Core Business Data)|
| (游客端 / Frontend)  |      |      (Backend)         |      +---------------------+
| +------------------+ |      |                        |
| | Map Service      | |      +------------------------+      +---------------------+
| | Adapter          | |                  |                   |    Redis Cache      |
| |------------------| |                  |------------------>| (Session, Hot Data) |
| | TencentMap Impl| |                  |                   +---------------------+
| | GaodeMap Impl  | |                  |
| | BaiduMap Impl  | |                  |                   +---------------------+
| +------------------+ |                  |                   |   Payment Gateway   |
+----------------------+                  |------------------>| (e.g., WeChat Pay)  |
                                          |                   +---------------------+
+----------------------+                  |
|                      |                  |
|  Web管理后台 (Vue)   |----------------->|
| (管理端 / Admin UI)  |
|                      |
+----------------------+
```

## 2. 技术选型

- **前端框架**: **Vue.js**。利用其生态（如uni-app或Taro）来开发微信小程序，实现高效的组件化开发和响应式界面。
- **后端框架**: **Spring Boot (Java)**。因其开发效率高、稳定性强、社区支持广泛，适合快速构建RESTful API。
- **数据库**:
    - **MySQL**: 作为主数据库，用于存储用户、景点、订单、活动等核心业务的结构化数据。
    - **Redis**: 作为缓存数据库，用于存储高频访问的数据、会话信息等。
- **地图服务**:
    - **模式**: 采用**适配器模式**在前端实现。
    - **实现**: 定义一个统一的地图服务接口 `IMapService`，并为腾讯地图、高德地图、百度地图分别创建实现该接口的适配器类。
    - **默认**: **腾讯地图SDK**。
- **开发工具**: VSCode, IntelliJ IDEA, Maven, Git。

## 3. 数据库设计

根据需求分析，定义以下核心数据实体。

### 3.1 ER图（概念模型）

```
[User] --1..*-- [Order]
[User] --1..*-- [Review]
[ScenicSpot] --1..*-- [Review]
[ScenicSpot] --1..*-- [Order]
[Activity]
[Admin]
```

### 3.2 核心实体属性

- **User (用户)**
    - `user_id` (PK, BIGINT): 用户ID
    - `openid` (VARCHAR): 微信用户唯一标识
    - `nickname` (VARCHAR): 昵称
    - `avatar_url` (VARCHAR): 头像地址
    - `map_preference` (VARCHAR): 地图偏好 ('Tencent', 'Gaode', 'Baidu')
    - `created_at` (DATETIME): 创建时间

- **ScenicSpot (景点)**
    - `spot_id` (PK, INT): 景点ID
    - `name` (VARCHAR): 景点名称
    - `address` (VARCHAR): 地址
    - `description` (TEXT): 详细介绍
    - `ticket_price` (DECIMAL): 门票价格
    - `opening_hours` (VARCHAR): 开放时间
    - `panorama_url` (VARCHAR): 3D全景图地址
    - `tags` (JSON or VARCHAR): 文化标签 (如 '["红色", "自然风光"]')
    - `created_at` (DATETIME): 创建时间

- **Activity (活动)**
    - `activity_id` (PK, INT): 活动ID
    - `title` (VARCHAR): 活动名称
    - `content` (TEXT): 活动详情
    - `start_time` (DATETIME): 开始时间
    - `end_time` (DATETIME): 结束时间
    - `location` (VARCHAR): 活动地点
    - `status` (VARCHAR): 状态 ('upcoming', 'ongoing', 'finished')

- **Order (订单)**
    - `order_id` (PK, BIGINT): 订单ID
    - `user_id` (FK, BIGINT): 用户ID
    - `spot_id` (FK, INT): 景点ID
    - `order_status` (VARCHAR): 订单状态 ('pending', 'paid', 'used', 'cancelled')
    - `total_amount` (DECIMAL): 总金额
    - `quantity` (INT): 数量
    - `created_at` (DATETIME): 创建时间

- **Admin (管理员)**
    - `admin_id` (PK, INT): 管理员ID
    - `username` (VARCHAR): 用户名
    - `password_hash` (VARCHAR): 加密后的密码
    - `role` (VARCHAR): 角色 ('super_admin', 'content_editor')

## 4. 模块设计

### 4.1 后端API模块 (Spring Boot)

后端的服务变得更纯粹，主要负责业务数据，不再处理地图相关的计算。

#### 4.1.1 `spot-service` (景点服务)
- `GET /api/spots`: 获取景点列表（支持分页、按标签筛选）。
- `GET /api/spots/{id}`: 获取单个景点详情。
- `GET /api/routes/recommended`: 获取系统预设的推荐路线（仅包含景点ID序列）。

#### 4.1.2 `order-service` (订单服务)
- `POST /api/orders`: 创建新订单。
- `GET /api/orders/user/{userId}`: 获取指定用户的订单列表。
- `POST /api/orders/{id}/pay`: 处理订单支付回调。

#### 4.1.3 `user-service` (用户服务)
- `POST /api/auth/login`: 处理用户微信登录，返回token。
- `GET /api/users/me`: 获取当前登录用户的信息。
- `PUT /api/users/me/preference`: 更新用户的地图偏好设置。

#### 4.1.4 `admin-service` (后台管理服务)
- `POST /api/admin/auth/login`: 管理员登录。
- `GET /api/admin/stats`: 获取统计数据。
- `POST /api/admin/content/review`: 审核内容。
- `GET /api/admin/users`: 获取用户列表。
- `POST /api/admin/policies`: 发布政策通知。

### 4.2 前端模块 (Vue)

前端将包含处理地图服务切换的逻辑。

#### 4.2.1 `map-adapter` (地图服务适配层)
- **IMapService.js (接口定义)**
    - `getCurrentLocation()`
    - `planRoute(start, end, waypoints, options)`
    - `startNavigation(route)`
- **TencentMap.js (腾讯地图实现)**: 实现 `IMapService` 接口，调用腾讯地图小程序SDK。
- **GaodeMap.js (高德地图实现)**: 实现 `IMapService` 接口，调用高德地图小程序SDK。
- **BaiduMap.js (百度地图实现)**: 实现 `IMapService` 接口，调用百度地图小程序SDK。
- **MapFactory.js (工厂)**: 根据用户偏好或默认设置，返回一个具体的地图服务实例。

#### 4.2.2 游客端视图
- **components/**:
    - `SpotCard.vue`: 景点卡片组件。
    - `RouteResult.vue`: 路线方案展示组件。
    - `MapViewer.vue`: 地图和导航视图组件，通过 `MapFactory` 与地图服务交互。
- **views/**:
    - `Home.vue`: 首页。
    - `SpotList.vue`: 景点列表页。
    - `SpotDetail.vue`: 景点详情页。
    - `RoutePlanner.vue`: 路线规划页。
    - `Profile.vue`: 个人中心。
    - `Settings.vue`: 设置页面，包含地图服务商选择。

#### 4.2.3 管理端视图
- (与之前设计相同)

## 5. 测试策略

- **单元测试**: 对后端Service层和前端地图适配器模块进行单元测试。
- **集成测试**: 测试API接口的正确性。
- **功能测试（黑盒）**: 针对每一种地图SDK（腾讯、高德、百度），都需要完整测试定位、路线规划和导航等核心功能。
- **性能测试**: 使用JMeter等工具模拟多用户并发访问。
- **兼容性测试**: 在不同型号的iOS和Android手机上测试小程序的UI显示和功能。
